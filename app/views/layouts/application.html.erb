<!DOCTYPE html>
<!-- paulirish.com/2008/conditional-stylesheets-vs-css-hacks-answer-neither/ -->
<!--[if lt IE 7 ]> <html class="ie6" lang="en"> <![endif]-->
<!--[if IE 7 ]>    <html class="ie7" lang="en"> <![endif]-->
<!--[if IE 8 ]>    <html class="ie8" lang="en"> <![endif]-->
<!--[if (gte IE 9)|!(IE)]><!--> <html lang="en"> <!--<![endif]-->
<head>
    <meta http-equiv="X-UA-COMPATIBLE" content="IE=edge" />
	<meta charset="utf-8" />
	<!-- Set the viewport width to device width for mobile -->
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <% if content_for?(:canonical) %>
	  <link rel="canonical" href="<%= request.protocol + request.host + yield(:canonical) %>" />
    <% end %>
	<title><%= content_for?(:title) ? yield(:title) : 'Motor Car Export' %></title>
    <link rel="icon" type="image/png" href="<%= image_path('favicon.ico') %>"/>
  <!--[if lt IE 9]>
	<%= javascript_include_tag "html5shiv" %>
  <![endif]-->
	<%= stylesheet_link_tag    "application", media: 'all' %>
    <%= javascript_include_tag "respond.min" %>
	<%= javascript_include_tag "custom.modernizr.dev" if Rails.env == 'development' %>
	<%= csrf_meta_tags %>
</head>
<body class="<%= params[:action] %> <%= params[:controller] %>">
	<div class="topbar">
		<h1 class="right" small-text="586.854.8781">Call: 586.854.8781</h1>
		<a href="/"><h1>Motor Car Export</h1></a>
	</div>
	<div class="topbar subnav">
		<nav>
		  <ul>
	        <li class="inventory"><%= link_to 'Vehicles', inventory_vehicles_path %></li>
		    <li class=""><%= link_to 'About', '/about' %></li>
	        <li class=""><%= link_to 'Buyer Info', '/buyer-info' %></li>
	        <li class=""><%= link_to 'FAQ', '/faq' %></li>
	        <li class=""><%= link_to 'Contact', '/contact' %></li>
	        <% if current_user %>
	            <li class="right"><span class="a">Hello, <%= current_user.login.capitalize %></span> &bull; <%= link_to 'Member\'s Area', '/members'%></li>
		    <% end %>
		  </ul>
		</nav>
	</div>
	<% if content_for? :featured and Vehicle.where(featured: true).any? %>
	  <div id="featured-box">
	    <%= render 'vehicles/featured' %>
	  </div>
	  <% end %>
	<div id="container" class="row">
	  <article id="content" class="large-12 columns <%= 'pushed-down' if content_for? :featured and Vehicle.where(featured: true).any?%>">
	    <%= render partial: 'layouts/breadcrumbs' if current_user %>
	    <%= yield %>
	    <div class="row">
	      <footer id="container-footer" class="twelve columns">&copy; <%= Time.new.year %> Motor Car Export</footer>
	    </div>
	  </article>

	</div>
	<%= render yield(:modal) if content_for? :modal %>
	<%= javascript_include_tag "application" %>
</body>
</html>
